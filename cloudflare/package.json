{
  "name": "paracheck-control-plane",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:local": "wrangler d1 execute paracheck --local --file=schema.sql",
    "db:remote": "wrangler d1 execute paracheck --remote --file=schema.sql"
  },
  "dependencies": {
    "@clerk/backend": "^3.18.1",
    "@cloudflare/containers": "^0.3.7",
    "jose": "^6.2.12"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.6.3",
    "vitest": "^3.2.0",
    "wrangler": "^4.135.0"
  }
}
